# Slither exports and analyzer artifacts
crytic-export/*
# Sample and interim output files
examples/*
# Local helper commands/history (user-specific)
.commands
# Local secrets and RPC settings
.env
# AI assistant guidance (local only)
AGENTS.md
PLAN.md
# User-local scratch files such as custom templates
local-only/
# Python bytecode cache
__pycache__/
# pytest cache
.pytest_cache/
# Ignore generated artifacts in src/; only keep the entry-point HTML tracked
src/*
!src/index.html
!src/favicon.svg
!src/hotkeys.json
latest_changes.txt
